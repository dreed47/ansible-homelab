---
all:
  children:
    proxmox_servers:
      hosts:
        proxmox-01:
          ansible_host: 192.168.86.40
          ansible_user: root
          # If using SSH key (recommended)
          # ansible_ssh_private_key_file: ~/.ssh/id_rsa
          # If using password (less secure)
          # ansible_ssh_pass: your_password
